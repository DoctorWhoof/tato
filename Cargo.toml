[package]
name = "tato"
version = "0.2.2"
edition = "2024"
license = "MIT"
description = "A ridiculously obsolete collection of zero-dependency game development modules that can run on any computer, even potato-powered ones!"
repository = "https://github.com/DoctorWhoof/tato"
categories = ["no-std"]

[dependencies]
tato_pad = { path = "pad" }
tato_layout = { path = "layout" }
tato_audio = { path = "audio" }
tato_video = { path = "video" }
tato_math = { path = "math" }
smooth-buffer = { path = "../smooth_buffer" }
# tato_arena = { path = "arena" }

[build-dependencies]
tato_pipe = { path = "pipeline" }

[workspace]
members = [
    "examples/scenes",
    "examples/audio",
    "examples/font",
    "examples/tilemap",
    "examples/palette_save",
    # "examples/pipe",
]
